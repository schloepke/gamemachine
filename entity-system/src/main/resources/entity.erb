package com.game_machine.entity_system.generated;

import java.util.HashMap;

public class Entity {

	<%- messages.each do |message| -%>
	private <%=message.getName%> <%=varname(message.getName)%>;
	<%- end -%>
	private final Integer id;
	private String clientId;
	
	public Entity(Integer id) {
		this.id = id;
	}
		
	public Integer getId() {
		return this.id;
	}
	
	public String getClientId() {
		return this.clientId;
	}
	
	public void setClientId(String clientId) {
		this.clientId = clientId;
	}
	
	<%- messages.each do |message| -%>
	// <%=message.getName%>
	public void set<%=message.getName%>(<%=message.getName%> <%=varname(message.getName)%>) {
		if (<%=varname(message.getName)%>.hasEntityId()) {
			<%=varname(message.getName)%> = <%=varname(message.getName)%>.clone();
		}
		
		this.<%=varname(message.getName)%> = <%=varname(message.getName)%>;
		this.<%=varname(message.getName)%>.setEntityId(this.getId());
	}
	
	public void remove<%=message.getName%>() {
		this.<%=varname(message.getName)%> = null;
	}
	
	public <%=message.getName%> get<%=message.getName%>() {
		return this.<%=varname(message.getName)%>;
	}
	
	public Boolean has<%=message.getName%>() {
		return (this.<%=varname(message.getName)%> != null);
	}
	
	
	
	<%- end -%>
	
	

}
	