<%= form_for(@data_editor) do |f| %>
  <% if @data_editor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_editor.errors.count, "error") %> prohibited this data_editor from being saved:</h2>

      <ul>
      <% @data_editor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
