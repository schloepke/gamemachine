GAME_MACHINE_ROOT = File.expand_path(File.join(Rails.root,'../server'))

#jars = Dir[File.join(GAME_MACHINE_ROOT, 'java/lib', 'game_machine*.jar')]
#jars += Dir[File.join(GAME_MACHINE_ROOT, 'java/lib', 'proto*.jar')]
#jars.each {|jar| require jar}

