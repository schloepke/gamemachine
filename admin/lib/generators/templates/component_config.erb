RailsAdmin.config do |config|
  config.model 'GameData::<%=@class_name%>' do
    configure :<%=@join_model_name%> do
      visible(false)
    end
    configure :entities do
      visible(false)
    end

    parent GameData::Entity
  end
end
